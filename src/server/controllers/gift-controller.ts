import { NextFunction } from "express";
const db = require('../models/db');

const giftController = {

  getWishList: async (req: Request, res: Response, next: NextFunction) => {
    // get label, description, and link for each item in the wishlist and save as an array of objects
    // query database for wishlist items associated with userId
  }



};

